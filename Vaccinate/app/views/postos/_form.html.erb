<%= form_with(model: posto, local: true) do |form| %>
  <% if posto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(posto.errors.count, "error") %> prohibited this posto from being saved:</h2>

      <ul>
      <% posto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id %>
    <%= form.number_field :id, id: :posto_id %>
  </div>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome, id: :posto_nome %>
  </div>

  <div class="field">
    <%= form.label :endereco %>
    <%= form.text_field :endereco, id: :posto_endereco %>
  </div>

  <div class="field">
    <%= form.label :latitude %>
    <%= form.text_field :latitude, id: :posto_latitude %>
  </div>

  <div class="field">
    <%= form.label :longitude %>
    <%= form.text_field :longitude, id: :posto_longitude %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
